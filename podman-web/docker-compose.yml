services:
  podman-web:
    build: .
    container_name: podman-web

    privileged: true

    tmpfs:
      - /home/poduser/.local/share/containers:uid=1000,gid=1000,mode=0755

    ports:
      - "9876:3000"
