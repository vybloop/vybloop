services:
  loop:
    build: .
    privileged: true
    network_mode: host
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      - ANTHROPIC_API_KEY
      - PORT
    volumes:
      - ./data:/data
      - podman_storage:/home/poduser/.local/share/containers
      - claude_config:/claudeconfig

volumes:
  podman_storage:
  claude_config:
